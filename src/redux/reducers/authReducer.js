const initState = {
    user: null
}

export const authReducer = (state=initState, action)=>{
    switch (action.type) {
        
        // case REGISTER_USER:
        //   return {...state}
        default:
           return state;
    }
}